version: '3.8'

services:
  streamlit-app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - OLLAMA_URL=http://ollama:8000
    depends_on:
      - ollama

  ollama:
    image: ollama/ollama:latest
    ports:
      - "8000:8000"